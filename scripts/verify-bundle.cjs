const fs = require('node:fs')
const path = require('node:path')
const c = fs.readFileSync(path.join(__dirname, '..', 'lib', 'client.js'), 'utf8')
console.log('bundle head ok:', c.startsWith('window.__ModuleLoader__.load({ id: "dsh-weather"'))
console.log('bytes:', c.length)
console.log('has WeatherBar:', c.includes('WeatherBar'))
console.log('seats referenced:', c.includes('conversation.session.header.actions') && c.includes('settings.section'))
